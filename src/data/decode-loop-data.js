export const PROMPT_TOKENS = ['The', 'quick', 'brown', 'fox'];
export const GENERATED_TOKENS = ['jumped', 'over', 'the', 'lazy', 'dog'];

export const phases = [
  {
    id: 'arrive',
    label: 'Prompt',
    title: 'A prompt arrives',
    caption:
      'The user sends "The quick brown fox," four tokens. To continue the sequence, the model runs each token through the transformer stack so attention can use the full prefix; the KV cache later stores the keys and values so decode does not repeat that full cost.',
    generated: 0,
    kvEntries: 0,
    kvSaved: 0,
    passes: 0,
    activePass: false,
    loopArrow: false,
    insight: null,
    candidates: null,
  },
  {
    id: 'prefill',
    label: 'Prefill',
    title: 'Prefill: process the whole prompt at once',
    caption:
      'All four prompt tokens enter the model in one prefill forward pass. Attention runs over the full prompt length and the KV cache stores the resulting key/value blocks for those positions.',
    generated: 0,
    kvEntries: 4,
    kvSaved: 0,
    passes: 1,
    activePass: true,
    loopArrow: false,
    insight: 'KV slots for the prompt are filled once; later decode steps read them instead of recomputing those positions.',
    candidates: [
      { token: 'jumped', prob: 0.31, chosen: true },
      { token: 'ran', prob: 0.18 },
      { token: 'leaped', prob: 0.12 },
      { token: 'walks', prob: 0.08 },
      { token: 'sat', prob: 0.06 },
    ],
  },
  {
    id: 'token-1',
    label: 'Token 1',
    title: 'First token: "jumped"',
    caption:
      'The model sampled "jumped," the first token the user sees (TTFT). The next forward uses that token as additional input along with the cached prefix.',
    generated: 1,
    kvEntries: 5,
    kvSaved: 4,
    passes: 2,
    activePass: false,
    loopArrow: true,
    insight: 'This step touched one new token while reusing five cached positions, so four full re-runs of the prefix were avoided.',
    candidates: [
      { token: 'over', prob: 0.42, chosen: true },
      { token: 'up', prob: 0.15 },
      { token: 'across', prob: 0.11 },
      { token: 'onto', prob: 0.09 },
      { token: 'into', prob: 0.05 },
    ],
  },
  {
    id: 'token-2',
    label: 'Token 2',
    title: 'The chain grows: "over"',
    caption:
      'Each new token extends the sequence; the runner still launches one forward per token, but the KV cache limits active computation to the latest position plus attention into stored states.',
    generated: 2,
    kvEntries: 6,
    kvSaved: 9,
    passes: 3,
    activePass: true,
    loopArrow: true,
    insight: 'Compared to no cache, this step avoids five full recomputations of earlier positions (nine cumulative over prior decode steps).',
    candidates: [
      { token: 'the', prob: 0.58, chosen: true },
      { token: 'a', prob: 0.14 },
      { token: 'that', prob: 0.06 },
      { token: 'some', prob: 0.04 },
      { token: 'his', prob: 0.03 },
    ],
  },
  {
    id: 'token-3',
    label: 'Token 3',
    title: 'Every token depends on the one before it',
    caption:
      'Autoregressive decoding keeps chaining: each sampled token becomes context for the next. Generation stops when the run hits an EOS rule or a stop condition, not from shortening the dependency chain.',
    generated: 3,
    kvEntries: 7,
    kvSaved: 15,
    passes: 4,
    activePass: true,
    loopArrow: true,
    insight: 'Seven positions are cached; one new token was computed this step, avoiding six redundant full passes (fifteen cumulative savings so far).',
    candidates: [
      { token: 'lazy', prob: 0.35, chosen: true },
      { token: 'sleeping', prob: 0.12 },
      { token: 'old', prob: 0.10 },
      { token: 'big', prob: 0.08 },
      { token: 'fence', prob: 0.07 },
    ],
  },
  {
    id: 'pattern',
    label: 'Summary',
    title: 'Prefill vs decode',
    caption:
      'Prefill fills KV for the whole prompt in one or a few forwards (chunked when the scheduler splits work). Decode then adds one token at a time, each step with its own forward while reusing stored KV. A 500-token answer needs on the order of 500 decode forwards after prefill; vLLM focuses on making each of those steps cheap.',
    generated: 5,
    kvEntries: 9,
    kvSaved: 30,
    passes: 6,
    activePass: false,
    loopArrow: true,
    insight: 'Across five decode steps, 30 full-prefix recomputations were avoided versus a naive recompute-from-scratch baseline; length scales that difference quickly.',
    candidates: null,
  },
];
