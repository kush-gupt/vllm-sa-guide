export const PROMPT_TOKENS = ['The', 'quick', 'brown', 'fox'];
export const GENERATED_TOKENS = ['jumped', 'over', 'the', 'lazy', 'dog'];

export const phases = [
  {
    id: 'arrive',
    label: 'Prompt',
    title: 'A prompt arrives',
    caption:
      'The user sends "The quick brown fox," four tokens. The model needs to continue this sequence, but it can\'t just look at the text. It must process every token through the full neural network to build an internal representation.',
    generated: 0,
    kvEntries: 0,
    kvSaved: 0,
    passes: 0,
    activePass: false,
    loopArrow: false,
    insight: null,
    candidates: null,
  },
  {
    id: 'prefill',
    label: 'Prefill',
    title: 'Prefill: process the whole prompt at once',
    caption:
      'All 4 prompt tokens enter the model in parallel. This is the "prefill" phase, the expensive first step. The model computes attention across every token and stores Key/Value pairs in the KV cache. One forward pass, 4 tokens\' worth of state saved.',
    generated: 0,
    kvEntries: 4,
    kvSaved: 0,
    passes: 1,
    activePass: true,
    loopArrow: false,
    insight: 'The KV cache is being populated. This is a one-time cost. Every subsequent token will read from it instead of recomputing these 4 representations.',
    candidates: [
      { token: 'jumped', prob: 0.31, chosen: true },
      { token: 'ran', prob: 0.18 },
      { token: 'leaped', prob: 0.12 },
      { token: 'walks', prob: 0.08 },
      { token: 'sat', prob: 0.06 },
    ],
  },
  {
    id: 'token-1',
    label: 'Token 1',
    title: 'First token: "jumped"',
    caption:
      'The model produced "jumped," the first token the user sees (TTFT). To predict what comes next, it must feed "jumped" back as input. The next token cannot exist without this one.',
    generated: 1,
    kvEntries: 5,
    kvSaved: 4,
    passes: 2,
    activePass: false,
    loopArrow: true,
    insight: 'KV cache in action: the model processed only 1 token instead of all 5. That\u2019s 4 recomputations avoided this step alone.',
    candidates: [
      { token: 'over', prob: 0.42, chosen: true },
      { token: 'up', prob: 0.15 },
      { token: 'across', prob: 0.11 },
      { token: 'onto', prob: 0.09 },
      { token: 'into', prob: 0.05 },
    ],
  },
  {
    id: 'token-2',
    label: 'Token 2',
    title: 'The chain grows: "over"',
    caption:
      '"over" depends on "jumped", which depended on the original prompt. Each token extends a chain, and the model must run a full forward pass for every link. The KV cache means only the newest token needs processing.',
    generated: 2,
    kvEntries: 6,
    kvSaved: 9,
    passes: 3,
    activePass: true,
    loopArrow: true,
    insight: 'Without KV cache, this step would reprocess all 6 tokens. With it: just 1. That\u2019s 5 recomputations skipped, 9 total so far.',
    candidates: [
      { token: 'the', prob: 0.58, chosen: true },
      { token: 'a', prob: 0.14 },
      { token: 'that', prob: 0.06 },
      { token: 'some', prob: 0.04 },
      { token: 'his', prob: 0.03 },
    ],
  },
  {
    id: 'token-3',
    label: 'Token 3',
    title: 'Every token depends on the one before it',
    caption:
      '"the" depends on "over", which depends on "jumped", and so on back to the prompt. This is autoregressive generation: an unbreakable chain where every token is built on the one before it. One forward pass per link, every link, until the response is complete.',
    generated: 3,
    kvEntries: 7,
    kvSaved: 15,
    passes: 4,
    activePass: true,
    loopArrow: true,
    insight: 'The KV cache now holds 7 entries. This step processed 1 token instead of 7, so 6 skipped, 15 total. The savings grow with every token.',
    candidates: [
      { token: 'lazy', prob: 0.35, chosen: true },
      { token: 'sleeping', prob: 0.12 },
      { token: 'old', prob: 0.10 },
      { token: 'big', prob: 0.08 },
      { token: 'fence', prob: 0.07 },
    ],
  },
  {
    id: 'pattern',
    label: 'The pattern',
    title: 'The pattern',
    caption:
      'Prefill processed all prompt tokens at once, parallel and compute-bound. Decode is the opposite: a strict chain where each token depends on its predecessor. For a 500-token reply, that means 500 sequential forward passes. The vLLM architecture exists to make each pass faster.',
    generated: 5,
    kvEntries: 9,
    kvSaved: 30,
    passes: 6,
    activePass: false,
    loopArrow: true,
    insight: 'Final tally: 30 token recomputations avoided across 5 decode steps. For a 500-token reply the KV cache prevents ~125,000 redundant computations.',
    candidates: null,
  },
];
